#include "main.h"
#include <stdlib.h>

/**
 * _abs -compute the absolute value of an integer
 * @n: integer to return the absolute value
 * Return: absolute value
 */

int _abs(int n)
{
	return (abs(n));
}
