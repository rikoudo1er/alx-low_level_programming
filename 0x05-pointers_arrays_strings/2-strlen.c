#include "main.h"
#include <string.h>

/**
 * _strlen- returns the length of a string.
 * @s: string to return the length
 * Return: the length of @s
 */

int _strlen(char *s)
{
	return (strlen(s));

}
