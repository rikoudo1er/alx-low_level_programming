#include "main.h"
#include <stdio.h>

/**
 * _puts- prints a string, followed by a new line, to stdout.
 * @str: string to print.
 * Return: nothing
 */

void _puts(char *str)
{
	puts(str);
	/*_putchar('\n');*/

}
